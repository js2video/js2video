{
  "name": "@js2video/server",
  "author": "Tight Ship AB",
  "license": "MIT",
  "version": "0.0.4",
  "type": "module",
  "exports": "./src/index.js",
  "types": "./types/index.d.ts",
  "files": [
    "src",
    "dist",
    "types"
  ],
  "scripts": {
    "dev": "rimraf types && chokidar 'src/**/*' 'node_modules/@js2video/client/src/**/*' -c 'vite build'",
    "build": "vite build && rimraf types && tsc",
    "prepublishOnly": "pnpm build"
  },
  "dependencies": {
    "@js2video/client": "workspace:*",
    "puppeteer": "23.3.0",
    "uuid": "10.0.0"
  },
  "devDependencies": {
    "chokidar-cli": "3.0.0",
    "rimraf": "6.0.1",
    "typescript": "5.6.2",
    "vite": "5.4.5"
  }
}
